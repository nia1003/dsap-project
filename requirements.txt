# Core
numpy
matplotlib
scikit-learn

# API backend
fastapi
uvicorn[standard]

# UI (legacy Streamlit)
streamlit
plotly

# Animations (optional — requires system pangocairo >= 1.30.0)
# manim

# Speech embeddings (optional — only needed for real LibriSpeech extraction)
# speechbrain
# torchaudio
