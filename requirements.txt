# Core
numpy
matplotlib
scikit-learn

# API backend
fastapi
uvicorn[standard]

# UI (legacy Streamlit)
streamlit
plotly

# Animations (requires system lib: apt-get install libpangocairo-1.0-0)
# manim

# Speech embeddings (optional — only needed for real LibriSpeech extraction)
# speechbrain
# torchaudio
